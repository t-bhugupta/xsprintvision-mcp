# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: sprint-insights-mcp
metadata:
  template: azd-init@1.15.1
services:
  sprint-insights-mcp:
    project: .
    host: containerapp
    language: js
    docker:
      path: Dockerfile
